"""LSAP-1 — Literary Space Annotation Protocol, v1.

Two instruments that share a vocabulary but never share a wire: an analysis side
(`instrument`, `coordinates`) that reads prose, and a generative side (`engine`) that
writes it. The firewall between them (Charter Principle 4) is enforced in tests.
"""

__version__ = "0.1.0"
