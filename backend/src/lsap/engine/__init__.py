"""L6 — the generative engine (generative space).

FIREWALL (Charter Principle 4): nothing in this package may import from
`lsap.instrument` or `lsap.coordinates`. The engine generates from the operator basis
and constraint compiler, never from measured coordinates. See `tests/test_firewall.py`.
"""
